<h1>Talks#new</h1>
<p>Find me in app/views/talks/new.html.erb</p>
<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        新規投稿
      </h1>
    </div>
  </div>
</section>


<% if @talk.errors.any? %>
  <div class="notification is-danger">
    <h2>
      <%= pluralize(@talk.errors.count, "error" ) %> prohibited this object from being saved: not successfully
    </h2>
    <ul>
      <% @talk.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
        <% end %>
    </ul>
  </div>
  <% end %>

    <section class="section">
      <div class="container">
        <div class="columns is-centered">
          <div class="column is-6">
            <%= form_for @talk do |f| %>

              <div class="field">
                <%= f.label :body, "詳細" , class: "label" %>
                  <%= f.text_area :body, class: "textarea" %>
              </div>

              <%= f.submit '投稿' , class: "button is-success" %>
                <% end %>
          </div>
        </div>
      </div>
    </section>
